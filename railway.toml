# railway.toml — Konfigurasi Railway.app
# Build dilakukan via Dockerfile (bukan Nixpacks)
# Start command, migrations, dan seeding dihandle di docker/start.sh

[deploy]
healthcheckPath = "/up"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
